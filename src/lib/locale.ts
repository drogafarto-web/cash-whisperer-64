import { ptBR } from 'date-fns/locale';

// Locale padrão para date-fns
export const dateFnsLocale = ptBR;

// Opções padrão para formatação de data
export const defaultDateFormatOptions = {
  locale: ptBR
};

// Re-exportar ptBR para uso direto
export { ptBR };
